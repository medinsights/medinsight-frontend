apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: medinsights
  labels:
    app: frontend
    component: ui
spec:
  type: ClusterIP  # Internal only - accessed through Ingress
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend
